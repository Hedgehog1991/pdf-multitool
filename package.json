{
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.6",
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently npm:dev:client",
    "dev:client": "cd client && npm run dev"
  }
}
